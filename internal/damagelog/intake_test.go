package damagelog

import (
	"testing"
)

// func TestIntake(t *testing.T) {
// 	filename := "testdata/data.json"
// 	f, err := os.Open(filename)
// 	require.NoError(t, err)
//
// 	// ProcessData(f)
// 	t.Fail()
// }

func TestIntake2(t *testing.T) {
	// filename := "testdata/data2.json"
	// f, err := os.Open(filename)
	// require.NoError(t, err)
	// ctx := context.Background()
	// conn, err := pgx.Connect(ctx, "user=admin password=admin dbname=damagelog host=localhost port=5432 sslmode=disable")
	// if err != nil {
	// 	panic(err)
	// }
	// defer conn.Close(ctx)
	// queries := dal.New(conn)
	//
	// ProcessData(f, queries)
}
